/** The contract version this bundle was built against.
 *
 * Baked by Vite from src/optionality/service/contract.json — the very same file the API
 * reads — so the two cannot drift by being edited separately. See ADR 0006, and that file's
 * docstring for when to bump it.
 */
export const CONTRACT_VERSION: number = __CONTRACT_VERSION__;
